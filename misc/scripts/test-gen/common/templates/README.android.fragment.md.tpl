# {{MARKER}} — Android instrumented tests for {{PROJECT_NAME}}

## Run on device or emulator

```bash
source ../../misc/client-setup/env.sh   # from repo root
./gradlew :app:connectedAndroidTest
```

Smoke stub: `app/src/androidTest/.../SmokeInstrumentedTest.kt`
