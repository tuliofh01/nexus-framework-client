builds/client/ (legacy redirect)
================================

This path is obsolete. Client distributables now land under:

  builds/clients/NexusFrameworkClient-<version>/

Populate with:

  ./build_client.sh
  ./gradlew deployToBuildsClient

See ../clients/README.txt and ../LAYOUT.md.
